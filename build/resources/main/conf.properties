host = localhost
DBName = sakila

user = root
password = 123654

custom_table_name = cars

create_table_request = CREATE TABLE cars (ID int(6) NOT NULL,MODEL_NAME VARCHAR(45) NOT NULL,COLOR VARCHAR(45) NOT NULL,PRIMARY KEY (id))
insert_table_request = INSERT INTO cars (ID, MODEL_NAME, COLOR) VALUES (490, 'Renault Logan', 'Gray')
update_table_request = UPDATE cars SET COLOR = 'RED' WHERE ID='490'
join_table_request = SELECT ct.city, adr.address FROM city ct LEFT JOIN address adr ON ct.city_id=adr.city_id WHERE city='Abu Dhabi'

